import {
	Container,
	Wrapper,
	MainForm,
	Title,
	Input,
	MainButton,
	SecondaryButton,
} from '../components/AccountForm';


const Login = () => {
	const handleLogin = async (e) => {};
	return (
		<Container>
			<Wrapper></Wrapper>
		</Container>
	);
};

export default Login;
